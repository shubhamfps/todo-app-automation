testing # Todo App Automation

Instructions to run the app and tests.

Run UI Tests
cd ui-tests
pip install -r requirements.txt
pytest

Run API Tests via Postman
Import postman/todo_api_tests.postman_collection.json into Postman
Then run it manually or using Newman
